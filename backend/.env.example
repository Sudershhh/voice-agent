# ============================================
# Paradise Voice Agent - Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ============================================
# LiveKit Configuration (REQUIRED)
# ============================================
# Get these from your LiveKit Cloud dashboard: https://cloud.livekit.io
# Or configure for self-hosted LiveKit server
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# ============================================
# OpenAI Configuration (REQUIRED)
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
# Used for:
# - Speech-to-Text (STT) via Whisper
# - Text embeddings for RAG (text-embedding-3-small)
# - Language model (GPT-3.5-turbo)
OPENAI_API_KEY=sk-your-openai-api-key

# ============================================
# Pinecone Configuration (REQUIRED for RAG)
# ============================================
# Get your API key from: https://app.pinecone.io/
# Used for vector storage of PDF content
PINECONE_API_KEY=your-pinecone-api-key
# Note: PINECONE_ENVIRONMENT is usually not needed for serverless indexes
PINECONE_ENVIRONMENT=
# Index name (will be created automatically if it doesn't exist)
PINECONE_INDEX_NAME=paradise-travel-index

# ============================================
# Optional: SerpAPI (for Flight Prices Tool)
# ============================================
# Get your API key from: https://serpapi.com/
# Used for getting real-time flight prices
# Leave empty if you don't need flight price lookups
SERPAPI_API_KEY=your-serpapi-api-key

# ============================================
# Optional: Google Places API (for Places Search Tool)
# ============================================
# Get your API key from: https://console.cloud.google.com/
# Enable "Places API" in your Google Cloud project
# Used for searching cafes, restaurants, hotels, etc.
# Leave empty if you don't need places search
GOOGLE_PLACES_API_KEY=your-google-places-api-key


